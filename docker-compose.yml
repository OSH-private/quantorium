version: "3.9"

services:
  vue-app:
    build: .
    container_name: kvantorium
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
